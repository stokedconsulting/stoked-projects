{
  "name": "claude-projects-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "compile": "pnpm -r run compile"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
