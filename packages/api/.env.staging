# Staging Environment Configuration
NODE_ENV=staging
PORT=3000

# MongoDB Configuration
# Use MongoDB Atlas staging cluster
# MONGODB_URI will be set via SST Secret

# Authentication
# API_KEYS will be set via SST Secret

# Logging
LOG_LEVEL=info
