# Production Environment Configuration
NODE_ENV=production
PORT=3000

# MongoDB Configuration
# Use MongoDB Atlas production cluster
# MONGODB_URI will be set via SST Secret (from senvn)

# Authentication
# API_KEYS will be set via SST Secret (from senvn)

# Logging
LOG_LEVEL=warn
