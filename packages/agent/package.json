{
  "name": "@stoked-projects/agent",
  "version": "0.1.0",
  "description": "Autonomous agent loop for Claude AI project orchestration using Claude Agent SDK",
  "author": "Stoked Consulting",
  "private": true,
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "compile": "tsc --noEmit",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "*"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3",
    "vitest": "^2.1.8"
  }
}
